import tree from './tree';
import heap from './heap';
import linkedList from './linkedList';
import sort from './sort';

const main = () => {
  // tree.run();
  // heap.run();
  // linkedList.run();
  sort.run();
};

main();
